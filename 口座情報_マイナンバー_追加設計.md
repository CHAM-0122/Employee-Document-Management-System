# 口座情報・マイナンバー 追加設計

## 方針

`口座情報` と `マイナンバー` は、現在の従業員入力フローとは分離して実装する。

分離理由:

- 個人情報の中でも機微性が高い
- 店舗責任者に閲覧させない設計にしやすい
- 利用目的が明確に異なる
- 将来の権限制御と監査ログを作りやすい

## 推奨フロー

1. 基本情報入力
2. 従業員誓約書
3. SNS誓約書
4. 最終署名
5. 口座情報入力
6. マイナンバー入力

または、より安全にするなら

1. 基本情報 + 誓約書 + SNS誓約書 + 署名
2. 後日、別URLで口座情報
3. 後日、別URLでマイナンバー

## 推奨実装

### 口座情報

- 同一システム内で追加可能
- ただし別画面、別テーブル、別権限

### マイナンバー

- 可能なら別画面、別テーブル、別権限
- 理想は別サービスまたは最低でも別保存ロジック

## 画面設計

### 1. 口座情報入力画面

用途:

- 給与振込先の取得

入力項目:

- 銀行名
- 支店名
- 支店番号
- 口座種別
- 口座番号
- 口座名義カナ

UI要件:

- 数字項目は半角のみ
- 口座番号は入力時マスク検討
- 銀行名は選択式でも可
- 口座名義は全角カナ推奨

確認項目:

- 給与振込口座として使用することに同意します
- 本人口座であることを確認しました

### 2. マイナンバー入力画面

用途:

- 法定手続き用の番号取得

入力項目:

- マイナンバー12桁
- マイナンバー確認入力
- 利用目的確認チェック

UI要件:

- 12桁固定
- 表示マスク
- コピペ制限は任意
- 画面上で平文再表示しない

確認項目:

- 利用目的を確認しました
- 入力内容が正しいことを確認しました

### 3. 完了画面

用途:

- 受付完了

表示:

- 口座情報受付完了
- マイナンバー受付完了
- 管理者確認待ち

## 項目定義

### 口座情報

| 項目ID | 表示名 | 型 | 必須 | バリデーション | 備考 |
| --- | --- | --- | --- | --- | --- |
| bank_name | 銀行名 | string | はい | 1-100文字 | 選択式も可 |
| branch_name | 支店名 | string | はい | 1-100文字 | |
| branch_code | 支店番号 | string | はい | 3桁数字 | 半角数字 |
| account_type | 口座種別 | enum | はい | 定義値内 | `ordinary`, `checking`, `savings` 等 |
| account_number | 口座番号 | string | はい | 7桁数字を基本 | 銀行仕様に応じ調整可 |
| account_holder_kana | 口座名義カナ | string | はい | 1-120文字 | 全角カナ推奨 |
| bank_agree_usage | 給与振込利用への同意 | boolean | はい | trueのみ | |
| bank_confirm_own_account | 本人口座確認 | boolean | はい | trueのみ | |

### マイナンバー

| 項目ID | 表示名 | 型 | 必須 | バリデーション | 備考 |
| --- | --- | --- | --- | --- | --- |
| my_number | マイナンバー | string | はい | 12桁数字 | 平文保持しない |
| my_number_confirmation | 確認入力 | string | はい | 12桁数字、一致必須 | |
| my_number_purpose_agree | 利用目的同意 | boolean | はい | trueのみ | |
| my_number_accuracy_confirm | 入力確認 | boolean | はい | trueのみ | |

## enum定義

### account_type

| 値 | 表示名 |
| --- | --- |
| ordinary | 普通 |
| checking | 当座 |
| savings | 貯蓄 |

## DB追加設計

## 1. bank_accounts

用途:

- 振込口座の保持

カラム案:

| カラム名 | 型 | 必須 | 備考 |
| --- | --- | --- | --- |
| id | uuid | はい | PK |
| employee_intake_id | uuid | はい | FK |
| bank_name | varchar(100) | はい | |
| branch_name | varchar(100) | はい | |
| branch_code | varchar(3) | はい | |
| account_type | varchar(20) | はい | |
| encrypted_account_number | text | はい | 暗号化保存 |
| account_number_last4 | varchar(4) | はい | 表示用 |
| account_holder_kana | varchar(120) | はい | |
| agreed_usage | boolean | はい | |
| confirmed_own_account | boolean | はい | |
| submitted_at | timestamptz | はい | |
| created_at | timestamptz | はい | |
| updated_at | timestamptz | はい | |

補足:

- `account_number` は平文で持たない
- 一覧には `last4` のみ表示

## 2. my_number_records

用途:

- マイナンバーの保持

カラム案:

| カラム名 | 型 | 必須 | 備考 |
| --- | --- | --- | --- |
| id | uuid | はい | PK |
| employee_intake_id | uuid | はい | FK |
| encrypted_my_number | text | はい | 暗号化保存 |
| my_number_last4 | varchar(4) | はい | 表示用 |
| purpose_of_use_version | varchar(40) | はい | 文面版管理 |
| agreed_purpose | boolean | はい | |
| confirmed_accuracy | boolean | はい | |
| submitted_at | timestamptz | はい | |
| created_at | timestamptz | はい | |
| updated_at | timestamptz | はい | |

補足:

- 平文保存しない
- 表示は原則 `****1234` のみ

## 3. audit_logs 追加利用

用途:

- 口座情報、マイナンバー操作の記録

追加action候補:

- `bank_account_saved`
- `my_number_saved`
- `bank_account_viewed`
- `my_number_viewed`

## API追加設計

### 従業員向け

- `PATCH /api/public/intakes/{token}/bank-account`
- `PATCH /api/public/intakes/{token}/my-number`

### 管理者向け

- `GET /api/admin/intakes/{id}/bank-account`
- `GET /api/admin/intakes/{id}/my-number`

注意:

- `my-number` 取得APIは権限をさらに絞る
- `store_admin` は `my-number` 非閲覧が望ましい

## 権限設計

### 店舗責任者

- 基本情報閲覧可
- 誓約書閲覧可
- 口座情報閲覧不可または一部のみ
- マイナンバー閲覧不可

### 本部労務

- 基本情報閲覧可
- 口座情報閲覧可
- マイナンバー閲覧可

### 経理

- 口座情報閲覧可
- マイナンバー閲覧不要

## 表示制御

### 一覧画面

- 口座番号は表示しない
- マイナンバーは表示しない
- 入力済み/未入力のみ表示

### 詳細画面

- 口座番号はマスク表示
- マイナンバーは末尾4桁のみ、または非表示

## セキュリティ方針

### 口座情報

- DB保存時暗号化
- 画面表示は最小限
- ダウンロード制限

### マイナンバー

- DB保存時暗号化必須
- 表示権限の厳格化
- 監査ログ必須
- 可能なら別シークレットキー管理

## 実装順

1. `bank_accounts` テーブル追加
2. 口座情報画面追加
3. 口座情報API追加
4. 権限制御追加
5. `my_number_records` テーブル追加
6. マイナンバー画面追加
7. 暗号化実装
8. 監査ログ強化

## おすすめ

まずは `口座情報` から入れるのが現実的です。  
`マイナンバー` は最後に別画面・別権限で追加する方が安全です。
