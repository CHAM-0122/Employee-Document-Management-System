# 雇用契約更新フロー 共通仕様整理

## 目的

半年ごとの契約更新を、社員・有期社員・アルバイト/パートで共通運用できるようにする。

更新期間は以下の2区分で固定する。

- 1月〜6月
- 7月〜12月

## 基本方針

- 入社フローと雇用契約更新フローは分離する
- 更新フローでは `雇用契約書のみ` を扱う
- 誓約書、SNS誓約書、口座情報、マイナンバーは更新フローに含めない
- 1人の従業員に対して `複数回の更新履歴` を持てる構造にする
- 画面構成はできるだけ共通化し、表示項目だけ雇用区分で出し分ける

## 対象雇用区分

- 正社員
- 有期社員
- アルバイト・パート

## 共通画面構成

### Step 1 更新内容確認

全雇用区分で共通表示する。

- 更新対象期間
- 氏名
- 店舗名
- 雇用区分
- 契約開始日
- 契約終了日
- 更新ルール
- 就業場所
- 業務内容
- 所定の労働時間等
- 休日

### Step 2 雇用契約書本文確認

全雇用区分で共通のUIを使う。

- 本文をスクロール表示
- 最下部まで確認
- チェックボックス3つ
- 電子同意

### Step 3 署名

全雇用区分で共通。

- 氏名表示
- 更新対象期間表示
- 手書き署名
- 内容確認チェック
- 電子署名同意チェック

## 雇用区分ごとの表示差分

### 正社員

- 契約期間: `期間の定めなし`
- 契約終了日: 画面上は `期間の定めなし`
- 賃金表示:
  - 基本給
  - 職務手当
  - 通勤手当
  - 支払日
  - 支払方法

### 有期社員

- 契約開始日と契約終了日を表示
- 更新ルールを表示
- 賃金表示:
  - 基本給
  - 職務手当
  - 通勤手当
  - 支払日
  - 支払方法

### アルバイト・パート

- 契約開始日と契約終了日を表示
- 更新ルールを表示
- 賃金表示:
  - 時給
  - 通勤手当
  - 支払日
  - 支払方法

## 更新対象期間のルール

### 期間の生成ルール

- 実行日が1月〜6月なら当年の `1月1日〜6月30日`
- 実行日が7月〜12月なら当年の `7月1日〜12月31日`

### 管理画面での表示

- `2026年1月〜6月`
- `2026年7月〜12月`

### 例外対応

- 過去期間の再署名が必要な場合は、管理画面から対象期間を手動指定できるようにする余地を残す

## 管理画面で必要な操作

### 一覧画面

- 対象従業員の検索
- 雇用区分の表示
- 最新の更新期間の表示
- 更新状態の表示
- `更新フロー発行` ボタン

### 詳細画面

- 現在の雇用条件表示
- 更新対象期間の表示
- 雇用契約更新ページURL
- QRコード表示
- 更新履歴一覧

## 更新状態のステータス案

- `not_sent`
- `sent`
- `viewed`
- `signed`
- `expired`

## データ設計の考え方

現状の `employee_intakes` に直接ぶら下げるのではなく、更新履歴テーブルを分ける。

### テーブル案

`employment_contract_renewals`

- id
- employee_intake_id
- period_year
- period_half
- employment_category
- contract_start_date
- contract_end_date
- renewal_pattern_text
- work_location_name
- work_location_address
- duty_description
- job_position_code
- current_role_label
- shift_start_time
- shift_end_time
- break_minutes
- holiday_rule_text
- basic_salary_monthly
- duty_allowance_monthly
- hourly_wage
- commuting_allowance_monthly
- commuting_allowance_note
- pay_closing_day
- pay_date
- wage_payment_method
- document_version
- status
- sent_at
- viewed_at
- signed_at
- signature_file_path
- created_at
- updated_at

### 一意制約案

- `employee_intake_id + period_year + period_half`

## API案

### 従業員向け

- `GET /api/public/employment-contracts/{token}`
- `POST /api/public/employment-contracts/{token}/consent`
- `POST /api/public/employment-contracts/{token}/signature`
- `POST /api/public/employment-contracts/{token}/complete`

### 管理者向け

- `POST /api/admin/employment-contract-renewals`
- `GET /api/admin/employment-contract-renewals`
- `GET /api/admin/employment-contract-renewals/{id}`
- `PATCH /api/admin/employment-contract-renewals/{id}`

## QR運用

- 更新ページURLをそのままQR化する
- 店舗で従業員へ提示して、その場で署名してもらう
- メール未使用でも運用可能

## 今後の実装順

1. 現在の専用ページを仮UIとして維持
2. 更新履歴テーブルを追加
3. 専用トークンを更新履歴単位で発行
4. 管理画面に `更新発行` と `更新履歴一覧` を追加
5. 社員・有期社員・アルバイトで本文差分を最終調整

## 補足

今回の方向性では、`雇用契約更新フローは社員・アルバイト共通で1本` にできる。
違いは `本文` と `表示項目` のみで吸収する。
