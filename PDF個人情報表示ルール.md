# PDF個人情報表示ルール

提出控えPDFに含める個人情報は、閲覧者種別ごとに以下の通り制御する。

## 本人用PDF

- 基本情報、連絡先、提出書類、署名情報を表示する。
- 本人写真は表示する。
- 口座情報は表示しない。
- マイナンバーは表示しない。

## 管理者用PDF

- 基本情報、連絡先、提出書類、署名情報を表示する。
- 本人写真は表示する。
- 口座情報は、口座情報の閲覧権限がある管理者のみ表示する。
- マイナンバーは、マイナンバーの閲覧権限がある管理者のみ表示する。

## 実装方針

- 表示可否は `src/lib/pdf/receipt-privacy.ts` に集約する。
- PDF生成処理やHTMLプレビューは、この表示ルールを参照して出力内容を決定する。
- 本番運用では、写真はS3等のファイルストレージに保存し、DBにはファイルパスまたはURLのみを保存する。
